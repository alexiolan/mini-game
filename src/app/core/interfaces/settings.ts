export interface ISettings {
    SPEED_PER_MINUTE: number,
    FINISH_SCORE: number,
    giftSpawnTimeMin: number,
    giftSpawnTimeMax: number,
    bubbleSpawnTimeMin: number,
    bubbleSpawnTimeMax: number,
    giftSize: number,
    bubbleSize: number,
    playerSize: number,
    bubbleSpeed: number
}